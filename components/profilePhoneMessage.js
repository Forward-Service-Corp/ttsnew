import React from 'react';

function ProfilePhoneMessage({phone}) {
    return (
        <div className={`bg-lime-600 text-white p-4`}>
            TIP: You can use your mobile phone number to log in faster and easier if you save it in your profile below.
        </div>
    );
}

export default ProfilePhoneMessage;
